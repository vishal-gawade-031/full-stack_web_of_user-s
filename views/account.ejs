<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account Settings</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

    <div class="container mt-5">
        <div class="card shadow p-4">
            <h2 class="text-center">Account Settings</h2>

            <div class="mt-4">
                <h5 class="text-primary"><h3>Username: <%= currUser.username %></h3></h5>
                <form action="/user/editusername/<%=currUser.username %>" class="d-inline">
                    <button class="btn  btn-dark btn-sm">Edit Username</button>
                </form>
            </div>

            <div class="mt-3">
                <h5 class="text-secondary "><h3>Gmail</h3> <%= currUser.email %></h5>
                <form action="/user/addgmail/<%=currUser.username%>" class="d-inline"><br>
                    <button class="btn btn-dark btn-sm">Add Gmail</button>
                </form>
            </div>

            <div class="mt-3">
                <form action="/user/changepassword/<%=currUser.username %>">
                    <h3>Password</h3><button class="btn btn-danger btn-sm">Re set password</button>
                </form>
            </div>

            <form action="/user/feedback/<%=currUser.username%>" class="mt-4">
                <h5 class="text-dark">Feedback</h5>
                <div class="d-flex">
                    <input type="text" class="form-control form-control-sm me-2" placeholder="Add your feedback">
                    <button class="btn btn-success btn-sm">Submit</button>
                </div>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
